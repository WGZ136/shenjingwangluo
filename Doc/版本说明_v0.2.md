# 无人机集群视觉感知系统 - 版本说明 v0.2

## 版本信息
- **版本号**: v0.2
- **日期**: 2025-04-17
- **状态**: 开发中 / 待硬件连接测试

---

## 一、本版本做了什么

### 1. 核心功能实现

#### 视觉处理模块 (`drone_swarm_system/src/drone_vision_processor.py`)
- [x] 基于 dataclass 的 `DroneVisionProcessor` 类封装
- [x] 多摄像头图像采集与管理（支持 2-4 个摄像头）
- [x] 光流估计（Farneback 算法）
- [x] **神经网络深度估计（Monodepth2）**
- [x] 传统算法深度估计（梯度法）作为备用
- [x] 特征点提取（ORB）
- [x] 3D 点云生成
- [x] **Open3D 3D点云可视化** (新增)
- [x] 立体深度估计（前视+后视摄像头）

#### 主程序
- [x] `main.py` - 笔记本摄像头测试版本
  - 交互式菜单系统
  - 支持实时切换神经网络/传统算法
  - 完整的图像采集→处理→点云生成流程
  
- [x] `main_ESP32.py` - ESP32-CAM 版本（待硬件测试）
  - 集成 ESP32-CAM 双摄像头接收器
  - 支持实时视频流预览
  - 支持单帧采集和处理

#### 神经网络集成
- [x] Monodepth2 深度估计网络集成
- [x] 修复了 `depth_decoder.py` 的相对导入问题
- [x] 本地模型文件路径配置
- [x] CPU/CUDA 自动设备选择
- [x] 强制神经网络模式（失败时报错）

#### ESP32-CAM 接收器
- [x] 双摄像头视频流接收 (`camera_receiver.py`)
- [x] MJPEG 流解析
- [x] 连接状态监控
- [x] 自动重连机制

### 2. 文件结构整理

```
shenjingwangluo-main/
├── main.py                      # 笔记本摄像头测试版
├── main_ESP32.py               # ESP32-CAM 版本
├── test_neural_network.py      # 神经网络诊断脚本
├── requirements.txt            # Python依赖
├── config.yaml                 # 系统配置
├── Doc/                        # 文档目录
│   └── 版本说明_v0.2.md       # 本文件
├── core_algorithms/            # 核心算法库
│   ├── monodepth2/            # 深度估计网络
│   ├── RAFT/                  # 光流算法（预留）
│   └── mono+stereo_640x192/   # 预训练模型权重
├── drone_swarm_system/
│   ├── src/
│   │   ├── drone_vision_processor.py   # 视觉处理核心
│   │   ├── esp32_receiver/             # ESP32接收器
│   │   │   ├── camera_receiver.py
│   │   │   └── config.py
│   │   └── Visual_process/             # 其他同学贡献的视觉模块
│   ├── core/
│   └── tests/
└── venv/                       # 虚拟环境
```

### 3. 修复的问题
- [x] Monodepth2 导入路径问题
- [x] `depth_decoder.py` 相对导入修复 (`from ..layers import *`)
- [x] Windows 控制台 Unicode 字符编码问题
- [x] ESP32 模块动态导入问题

---

## 二、如何使用

### 环境准备

1. **安装依赖**
   ```bash
   # 创建虚拟环境
   python -m venv venv
   
   # 激活虚拟环境
   .\venv\Scripts\activate
   
   # 安装依赖
   pip install -r requirements.txt
   
   # 安装 PyTorch (根据CUDA版本选择)
   # CPU版本:
   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
   ```

2. **验证神经网络**
   ```bash
   .\venv\Scripts\python.exe test_neural_network.py
   ```

### 使用笔记本摄像头测试

```bash
.\venv\Scripts\python.exe main.py
```

**菜单选项**:
- `1` - 初始化会话
- `2` - 采集+处理图像（使用神经网络深度估计）
- `3` - 生成点云
- `4` - 执行完整流程
- `5` - 可视化结果（图像）
- `6` - **3D点云可视化**（需要安装 Open3D）
- `7` - 切换神经网络/传统算法
- `0` - 退出

**查看3D点云**:
1. 先安装 Open3D: `pip install open3d==0.18.0`
2. 运行程序，完成图像采集和处理（选项2）
3. 生成点云（选项3）
4. 选择 `6` - 3D点云可视化
5. 在弹出的Open3D窗口中：
   - 鼠标左键拖动：旋转视角
   - 鼠标右键拖动：平移
   - 滚轮：缩放
   - 按 `Q` 或关闭窗口退出

### 使用 ESP32-CAM（硬件连接后）

**前置条件**:
- ESP32-CAM 已上电并烧录固件
- 电脑连接到 ESP32-CAM 的 WiFi 热点
- 确认 config.py 中的 IP 地址正确

```bash
.\venv\Scripts\python.exe main_ESP32.py
```

**菜单选项**:
- `1` - 初始化 ESP32 会话
- `2` - 显示实时预览
- `3` - 采集图像
- `4` - 处理图像
- `6` - 执行完整流程
- `0` - 退出

---

## 三、待完成的工作

### 1. 硬件相关（等待焊锡连接）
- [ ] ESP32-CAM 电源连接和测试
- [ ] 双摄像头（前视0度+后视180度）布置和固定
- [ ] WiFi 连接稳定性测试
- [ ] 图像传输质量验证

### 2. 功能扩展
- [ ] 多无人机图像融合（模拟集群）
- [ ] 运动矩阵提取和位姿估计
- [ ] 集群控制算法接口
- [ ] 实时可视化界面优化

### 3. 性能优化
- [ ] CUDA 加速支持
- [ ] 模型量化优化
- [ ] 多线程图像处理

### 4. 文档完善
- [ ] API 文档
- [ ] 硬件连接指南
- [ ] 调参指南

---

## 四、已知问题

1. **ESP32 尚未连接**
   - 状态: 代码已就绪，等待硬件
   - 影响: main_ESP32.py 无法实际测试
   - 解决: 完成焊锡连接后测试

2. **CUDA 未配置**
   - 状态: 当前使用 CPU 推理
   - 影响: 深度估计速度较慢
   - 解决: 安装 CUDA 版本的 PyTorch

3. **Open3D 可选**
   - 状态: 当前未强制要求
   - 影响: 3D 可视化功能受限
   - 解决: 取消 requirements.txt 中的注释

---

## 五、依赖清单

### 核心依赖
- numpy==1.26.4
- opencv-python==4.8.1.78
- pillow==12.1.0
- matplotlib==3.10.8
- scipy==1.12.0
- PyYAML==6.0.3
- requests==2.32.0

### 神经网络
- torch (2.5.1 推荐)
- torchvision (0.20.1 推荐)

### 可选依赖
- open3d==0.18.0 (3D可视化)

---

## 六、项目参与者

- **机器视觉**: [你的名字]
- **集群控制**: [另一位同学]

---

## 七、下一步行动计划

1. **立即**: 确认代码无误后上传到仓库
2. **近期**: 完成 ESP32-CAM 硬件连接
3. **短期**: 测试 main_ESP32.py 功能
4. **中期**: 实现多无人机图像融合
5. **长期**: 集成集群控制算法

---

**备注**: 本版本主要完成软件框架搭建和神经网络集成，硬件测试待 ESP32 连接后进行。
